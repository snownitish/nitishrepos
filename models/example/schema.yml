
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: emp1
      description: "testing emp"
      columns:
          - name: EMP_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null       

    - name: emp2
      description: "testing sales"
      columns:
          - name: sale_id
            description: "The primary key  table"
            tests:
                - unique
                - not_null                      

